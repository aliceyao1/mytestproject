<!-- BEGIN FLOWAI -->
<!-- Generated by FlowAI. Edits inside this block are replaced on the next run;
     anything outside it is never touched. -->

## Project conventions (FlowAI)
- Language: golang
- FlowAI works on branch: flowai/add-a-go-hello-world-program-20260921-115857-d6ebf251

## Design record for this job (FlowAI)

This job's agreed requirements, design and prior verdicts are files in this workspace.
Read the ones that exist before writing, reviewing, or validating code, and treat them as
binding rather than as background:

  - `docs/REQUIREMENTS-add-a-go-hello-world-program.md` — what must be built, and the acceptance criteria it is judged against
  - `docs/ARCHITECTURE-add-a-go-hello-world-program.md` — the interfaces, constraints and boundaries the work must stay inside
  - `docs/IMPLEMENTATION-add-a-go-hello-world-program.md` — the plan for the implementation, and the engineer's account of what was done
  - `docs/PEER_REVIEW-add-a-go-hello-world-program.md` — the reviewer's findings and approval status
  - `docs/DOMAIN_REVIEW-add-a-go-hello-world-program.md` — the domain reviewer's findings
  - `docs/STRUCTURAL_VALIDATION-add-a-go-hello-world-program.md` — what the structural gate checked and found
  - `docs/ENGINEERING_QUALITY_VALIDATION-add-a-go-hello-world-program.md` — what the engineering-quality gate checked and found
  - `docs/GROUNDED_TRUTH_VALIDATION-add-a-go-hello-world-program.md` — whether the work was judged grounded in the goal
  - `docs/DOMAIN_VALIDATION-add-a-go-hello-world-program.md` — the domain gate's verdict
  - `docs/FINAL_DELIVERY-add-a-go-hello-world-program.md` — the final delivery readiness assessment

A `-001`, `-002` … suffix means a document was written more than once; the highest number
is the most recent, and the unsuffixed file may be one this repository already had.
A `-subtask1`, `-subtask2` … suffix on the implementation document records one slice of
the work; a `-remediation4` suffix is the plan for a round of fixes a quality gate asked
for, covering subtask 4 onward. Both belong to this job. The file listed above, without
either suffix, is the plan for the feature as a whole and is the one that binds.
Do not delete, empty, or overwrite them — they are deliverables in their own right.

Files matching these names but with **different goal keywords** belong to earlier jobs in
this workspace — a thread runs several jobs against one repository and each names its
documents after its own goal. They describe the code you are changing and are worth
reading. Their requirements are not your brief: do not implement them, and do not
contradict them silently — if this job requires departing from one, say which and why in
your report.

Every job numbers its criteria from one, so `AC-5` in an earlier job's document is a
different `AC-5` from this job's. Any criterion or requirement identifier you write into
code, comments, tests or a report must resolve against **this job's** requirements
document named above. If you need to refer to an earlier job's, name its document too.

If your task brief names a single document as the only file you may create or change,
that instruction stands for the whole session: produce that document and change nothing
else. Reading the code and running commands to understand it is expected; writing source,
tests, configuration or a scaffold is a later stage's work, not yours.

FlowAI delivers this job through git: it cut the working branch, it commits each stage's work after that stage finishes, and it opens the pull request. Do not run `git add`, `git commit`, `git push`, `git checkout` or any history-rewriting command yourself — a commit of your own splits the history FlowAI is assembling.
<!-- END FLOWAI -->
